<UserControl x:Class="WpfApp1.LeaseEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="700">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="180"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Text="Добавление договора аренды" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.ColumnSpan="2" Foreground="#2C5F7F"/>

            <!-- Номер договора -->
            <TextBlock Text="Номер договора:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtLeaseNumber" Grid.Row="1" Grid.Column="1" Margin="0,0,0,10"/>

            <!-- Выбор арендатора -->
            <TextBlock Text="Арендатор:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbTenant" Grid.Row="2" Grid.Column="1" DisplayMemberPath="Name" SelectedValuePath="TenantID" Margin="0,0,0,10"/>

            <!-- Выбор объекта -->
            <TextBlock Text="Объект недвижимости:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbProperty" Grid.Row="3" Grid.Column="1" DisplayMemberPath="Address" SelectedValuePath="PropertyID" Margin="0,0,0,10"/>

            <!-- Дата начала -->
            <TextBlock Text="Дата начала:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <DatePicker x:Name="DpStartDate" Grid.Row="4" Grid.Column="1" Margin="0,0,0,10"/>

            <!-- Дата окончания -->
            <TextBlock Text="Дата окончания:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <DatePicker x:Name="DpEndDate" Grid.Row="5" Grid.Column="1" Margin="0,0,0,10"/>

            <!-- Ежемесячная плата (автозаполняется из объекта) -->
            <TextBlock Text="Ежемесячная плата (₽):" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtMonthlyAmount" Grid.Row="6" Grid.Column="1" Margin="0,0,0,10" IsReadOnly="True" Background="#F0F0F0"/>

            <!-- Способ оплаты -->
            <TextBlock Text="Способ оплаты:" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbPaymentMethod" Grid.Row="7" Grid.Column="1" Margin="0,0,0,10">
                <ComboBoxItem Content="Банковский перевод"/>
                <ComboBoxItem Content="Наличные"/>
                <ComboBoxItem Content="Безналичный расчет"/>
            </ComboBox>

            <!-- Примечание -->
            <TextBlock Text="Примечание:" Grid.Row="8" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,0,5"/>
            <TextBox x:Name="TxtNotes" Grid.Row="8" Grid.Column="1" Height="80" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10"/>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal" Grid.Row="9" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,20,0,0">
                <Button Content="Сохранить и создать договор" Click="BtnSaveAndGenerate_Click" Width="220" Height="35" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}" Background="#4CAF50"/>
                <Button Content="Сохранить" Click="BtnSave_Click" Width="150" Height="35" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
                <Button Content="Отмена" Click="BtnCancel_Click" Width="150" Height="35" Style="{StaticResource TabButtonStyle}"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
