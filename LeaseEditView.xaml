<Window x:Class="WpfApp1.LeaseEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Редактирование договора аренды" Height="600" Width="900"
        WindowStartupLocation="CenterScreen" Background="#E8F4F8">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Редактирование договора аренды" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.ColumnSpan="2" Foreground="#2C5F7F"/>

        <!-- Номер договора -->
        <TextBlock Text="Номер договора:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtLeaseNumber" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Объект недвижимости -->
        <TextBlock Text="Объект недвижимости:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbProperty" Grid.Row="2" Grid.Column="1" Height="30" Margin="0,5" DisplayMemberPath="Address" SelectedValuePath="PropertyID"/>

        <!-- Арендатор -->
        <TextBlock Text="Арендатор:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbTenant" Grid.Row="3" Grid.Column="1" Height="30" Margin="0,5" DisplayMemberPath="Name" SelectedValuePath="TenantID"/>

        <!-- Дата начала -->
        <TextBlock Text="Дата начала:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DpStartDate" Grid.Row="4" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Дата окончания -->
        <TextBlock Text="Дата окончания:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <DatePicker x:Name="DpEndDate" Grid.Row="5" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Ежемесячная сумма -->
        <TextBlock Text="Ежемесячная сумма (₽):" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtMonthlyAmount" Grid.Row="6" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Статус -->
        <TextBlock Text="Статус:" Grid.Row="7" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbStatus" Grid.Row="7" Grid.Column="1" Height="30" Margin="0,5">
            <ComboBoxItem Content="Активен"/>
            <ComboBoxItem Content="Завершен"/>
            <ComboBoxItem Content="Расторгнут"/>
            <ComboBoxItem Content="На подписании"/>
        </ComboBox>

        <!-- Информация о платежах -->
        <Border Grid.Row="8" Grid.ColumnSpan="2" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="10" Margin="0,15,0,0">
            <StackPanel>
                <TextBlock Text="Последние платежи" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" Foreground="#2C5F7F"/>
                <DataGrid x:Name="PaymentsGrid" AutoGenerateColumns="False" Height="150" IsReadOnly="True" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Дата" Binding="{Binding PaymentDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                        <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat=N2}" Width="100"/>
                        <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="10" Grid.ColumnSpan="3" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="120" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="120" Style="{StaticResource TabButtonStyle}"/>
            <Button x:Name="BtnDeleteLease" Content="🗑️ Удалить договор" Click="BtnDeleteLease_Click" Width="120" Margin="10,0,0,0" Style="{StaticResource TabButtonStyle}" Visibility="Collapsed"  Background="#CD5C5C"/>
        </StackPanel>
    </Grid>
</Window>
