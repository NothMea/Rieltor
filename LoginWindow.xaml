<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Вход в систему" Height="570" Width="400"
        WindowStartupLocation="CenterScreen" Background="#E8F4F8">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,40">
            <Image Source="/Resources/logo.png" Width="90" Height="80" Margin="0,0,0,15"/>
            <TextBlock Text="АРЕНДА" FontSize="22" FontWeight="Bold" Foreground="#2C5F7F" HorizontalAlignment="Center"/>
            <TextBlock Text="-" FontSize="22" FontWeight="Bold" Foreground="#2C5F7F" HorizontalAlignment="Center"/>
            <TextBlock Text="УПРАВЛЕНИЕ" FontSize="22" FontWeight="Bold" Foreground="#2C5F7F" HorizontalAlignment="Center"/>
            <TextBlock Text="Вход в систему" FontSize="15" Foreground="#666666" HorizontalAlignment="Center" Margin="0,8,0,0"/>
        </StackPanel>

        <!-- Форма входа -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center" MaxWidth="300">
            <TextBlock Text="Логин:" Margin="0,0,0,5" Foreground="#333333" FontWeight="SemiBold"/>
            <TextBox x:Name="txtLogin"/>

            <TextBlock Text="Пароль:" Margin="0,1,0,5" Foreground="#333333" FontWeight="SemiBold"/>
            <PasswordBox Height="34" x:Name="txtPassword"/>

            <Button Content="Войти" Click="BtnLogin_Click" Height="42" FontSize="16" FontWeight="Bold" Margin="0,25,0,0" Style="{StaticResource TabButtonStyle}"/>
        </StackPanel>

        <!-- Футер -->
        <TextBlock Grid.Row="2" Text="© 2026 Система управления арендой" Foreground="#999999" FontSize="11" HorizontalAlignment="Center" Margin="0,0,0,10"/>
    </Grid>
</Window>
