<Window x:Class="WpfApp1.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Вход в систему" Height="510" Width="400"
        WindowStartupLocation="CenterScreen" Background="#E8F4F8">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,30">
            <Image Source="/Resources/logo.png" Width="80" Height="70" Margin="0,0,0,10"/>
            <TextBlock Text="АРЕНДА-УПРАВЛЕНИЕ" FontSize="20" FontWeight="Bold" Foreground="#2C5F7F" HorizontalAlignment="Center"/>
            <TextBlock Text="Вход в систему" FontSize="14" Foreground="#666666" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Форма входа -->
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <TextBlock Text="Логин:" Margin="0,10,0,0" Foreground="#333333" FontWeight="SemiBold"/>
            <TextBox x:Name="txtLogin" Height="35" Margin="0,0,0,15" BorderBrush="#CCCCCC"/>

            <TextBlock Text="Пароль:" Margin="0,10,0,0" Foreground="#333333" FontWeight="SemiBold"/>
            <PasswordBox x:Name="txtPassword" Height="35" Margin="0,0,0,20" BorderBrush="#CCCCCC"/>

            <Button Content="Войти" Click="BtnLogin_Click" Height="40" FontSize="16" FontWeight="Bold"/>
        </StackPanel>

        <!-- Футер -->
        <TextBlock Grid.Row="2" Text="© 2026 Система управления арендой" Foreground="#999999" FontSize="11" HorizontalAlignment="Center"/>
    </Grid>
</Window>
