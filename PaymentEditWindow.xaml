<Window x:Class="WpfApp1.PaymentEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление платежа" Height="500" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#E8F4F8">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Новый платеж" FontSize="24" FontWeight="Bold" Foreground="#2C5F7F" 
                   Grid.ColumnSpan="2" Margin="0,0,0,20"/>

        <!-- Выбор договора -->
        <TextBlock Text="Договор аренды:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox x:Name="CmbLease" Grid.Row="1" Grid.Column="1" DisplayMemberPath="DisplayText" 
                  SelectedValuePath="LeaseID" Margin="0,0,0,15"/>

        <!-- Дата платежа -->
        <TextBlock Text="Дата платежа:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <DatePicker x:Name="DpPaymentDate" Grid.Row="2" Grid.Column="1" Margin="0,0,0,15"/>

        <!-- Сумма -->
        <TextBlock Text="Сумма (₽):" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtAmount" Grid.Row="3" Grid.Column="1" Margin="0,0,0,15" PreviewTextInput="TxtAmount_PreviewTextInput"/>

        <!-- Статус -->
        <TextBlock Text="Статус:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,10"/>
        <ComboBox x:Name="CmbStatus" Grid.Row="4" Grid.Column="1" Margin="0,0,0,15">
            <ComboBoxItem Content="В ожидании"/>
            <ComboBoxItem Content="Оплачен"/>
            <ComboBoxItem Content="Просрочен"/>
        </ComboBox>

        <!-- Примечание -->
        <TextBlock Text="Примечание:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,0,10"/>
        <TextBox x:Name="TxtNotes" Grid.Row="5" Grid.Column="1" Height="80" TextWrapping="Wrap" 
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Margin="0,0,0,15"/>

        <!-- Кнопки -->
        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="150" Height="35" 
                    Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="150" Height="35" 
                    Style="{StaticResource TabButtonStyle}"/>
        </StackPanel>
    </Grid>
</Window>
