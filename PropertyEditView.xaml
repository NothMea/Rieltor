<Window x:Class="WpfApp1.PropertyEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Редактирование объекта недвижимости" Height="550" Width="650"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="180"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Редактирование объекта" FontSize="20" FontWeight="Bold" 
                   Grid.ColumnSpan="2" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <!-- Адрес -->
        <TextBlock Text="Адрес:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtAddress" Grid.Row="1" Grid.Column="1" Height="30" Margin="10,5"/>

        <!-- Тип объекта -->
        <TextBlock Text="Тип объекта:" Grid.Row="2" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbPropertyType" Grid.Row="2" Grid.Column="1" Height="30" Margin="10,5">
            <ComboBoxItem Content="Офис"/>
            <ComboBoxItem Content="Склад"/>
            <ComboBoxItem Content="Магазин"/>
            <ComboBoxItem Content="Производство"/>
            <ComboBoxItem Content="Другое"/>
        </ComboBox>

        <!-- Площадь -->
        <TextBlock Text="Площадь (м²):" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtArea" Grid.Row="3" Grid.Column="1" Height="30" Margin="10,5"
                 PreviewTextInput="TxtNumeric_PreviewTextInput"/>

        <!-- Статус -->
        <TextBlock Text="Статус:" Grid.Row="4" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbStatus" Grid.Row="4" Grid.Column="1" Height="30" Margin="10,5">
            <ComboBoxItem Content="Свободен"/>
            <ComboBoxItem Content="Арендован"/>
            <ComboBoxItem Content="На ремонте"/>
            <ComboBoxItem Content="Продан"/>
        </ComboBox>

        <!-- Арендная плата -->
        <TextBlock Text="Арендная плата (₽/мес):" Grid.Row="5" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtMonthlyRent" Grid.Row="5" Grid.Column="1" Height="30" Margin="10,5"
                 PreviewTextInput="TxtNumeric_PreviewTextInput"/>

        <!-- Путь к изображению -->
        <TextBlock Text="Изображение:" Grid.Row="6" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" Margin="10,5">
            <TextBox x:Name="TxtImagePath" Width="300" Height="30" IsReadOnly="True"/>
            <Button Content="Обзор..." Click="BtnBrowseImage_Click" Margin="10,0,0,0" Width="80"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="8" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="120" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="120"/>
        </StackPanel>
    </Grid>
</Window>
