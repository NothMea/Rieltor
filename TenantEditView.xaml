<Window x:Class="WpfApp1.TenantEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="Редактирование арендатора" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Редактирование арендатора" FontSize="20" FontWeight="Bold" 
                   Grid.ColumnSpan="2" Margin="0,0,0,20" HorizontalAlignment="Center"/>

        <!-- ФИО / Название организации -->
        <TextBlock Text="ФИО / Организация:" Grid.Row="1" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtName" Grid.Row="1" Grid.Column="1" Height="30" Margin="10,5"/>

        <!-- Телефон -->
        <TextBlock Text="Телефон:" Grid.Row="2" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtPhone" Grid.Row="2" Grid.Column="1" Height="30" Margin="10,5"
                 PreviewTextInput="TxtPhone_PreviewTextInput"/>

        <!-- ИНН -->
        <TextBlock Text="ИНН:" Grid.Row="3" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtINN" Grid.Row="3" Grid.Column="1" Height="30" Margin="10,5"
                 PreviewTextInput="TxtINN_PreviewTextInput"/>

        <!-- Email -->
        <TextBlock Text="Email:" Grid.Row="4" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtEmail" Grid.Row="4" Grid.Column="1" Height="30" Margin="10,5"/>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="6" Grid.ColumnSpan="2" 
                    HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="120" Margin="0,0,10,0"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="120"/>
        </StackPanel>
    </Grid>
</Window>
