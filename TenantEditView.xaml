<UserControl x:Class="WpfApp1.TenantEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="600">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Редактирование арендатора" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.ColumnSpan="2" Foreground="#2C5F7F"/>

        <!-- ФИО/Название -->
        <TextBlock Text="ФИО/Название:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtName" Grid.Row="1" Grid.Column="1"/>

        <!-- Телефон -->
        <TextBlock Text="Телефон:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtPhone" Grid.Row="2" Grid.Column="1">
            <TextBox.ToolTip>Формат: +7 (XXX) XXX-XX-XX (10-15 цифр)</TextBox.ToolTip>
        </TextBox>

        <!-- ИНН -->
        <TextBlock Text="ИНН:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtINN" Grid.Row="3" Grid.Column="1">
            <TextBox.ToolTip>10 цифр для организаций или 12 цифр для физических лиц</TextBox.ToolTip>
        </TextBox>

        <!-- Email -->
        <TextBlock Text="Email:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,0,5"/>
        <TextBox x:Name="TxtEmail" Grid.Row="4" Grid.Column="1">
            <TextBox.ToolTip>Пример: example@mail.ru</TextBox.ToolTip>
        </TextBox>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="5" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="180" Height="30" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="180" Height="30" Style="{StaticResource TabButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
