<UserControl x:Class="WpfApp1.Views.LeaseHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="История договоров аренды" FontSize="24" FontWeight="Bold" Foreground="#2C5F7F"/>
            <TextBlock Text="Архив завершенных и расторгнутых договоров" FontSize="14" Foreground="#666" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Фильтры -->
        <Border Grid.Row="0" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="15" Margin="0,60,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Фильтр по статусу:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbStatusFilter" Grid.Column="1" Width="150" Margin="0,0,10,0" SelectionChanged="CmbStatusFilter_SelectionChanged">
                    <ComboBoxItem Content="Все статусы" IsSelected="True"/>
                    <ComboBoxItem Content="Завершен"/>
                    <ComboBoxItem Content="Расторгнут"/>
                </ComboBox>

                <TextBlock Text="Поиск:" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="TxtSearch" Grid.Column="3" Width="150" Margin="0,0,10,0" TextChanged="TxtSearch_TextChanged"/>

                <TextBlock Text="Город:" Grid.Column="4" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox x:Name="TxtCitySearch" Grid.Column="5" Width="120" Margin="0,0,10,0" TextChanged="TxtCitySearch_TextChanged"/>

                <StackPanel Grid.Column="6" Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBlock Text="Период:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <DatePicker x:Name="DpFromDate" Width="100" Margin="0,0,5,0" SelectedDateChanged="DateFilter_Changed"/>
                    <TextBlock Text="—" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <DatePicker x:Name="DpToDate" Width="100" Margin="0,0,10,0" SelectedDateChanged="DateFilter_Changed"/>
                    <Button Content="Сбросить" Click="BtnResetFilters_Click" Width="200" Style="{StaticResource TabButtonStyle}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Таблица истории договоров -->
        <Border Grid.Row="1" Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="5" Padding="10">
            <DataGrid x:Name="HistoryGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False" 
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" 
                      AlternatingRowBackground="#F9F9F9" RowHeight="45">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="№ договора" Binding="{Binding LeaseNumber}" Width="100"/>
                    <DataGridTextColumn Header="Объект" Binding="{Binding PropertyAddress}" Width="200"/>
                    <DataGridTextColumn Header="Арендатор" Binding="{Binding TenantName}" Width="150"/>
                    <DataGridTextColumn Header="Дата начала" Binding="{Binding StartDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                    <DataGridTextColumn Header="Дата окончания" Binding="{Binding EndDate, StringFormat=dd.MM.yyyy}" Width="90"/>
                    <DataGridTextColumn Header="Сумма (₽)" Binding="{Binding MonthlyAmount, StringFormat=N0}" Width="90"/>
                    <DataGridTextColumn Header="Статус" Binding="{Binding OriginalStatus}" Width="100"/>
                    <DataGridTextColumn Header="Дата расторжения" Binding="{Binding TerminationDate, StringFormat=dd.MM.yyyy}" Width="100"/>
                    <DataGridTextColumn Header="Причина" Binding="{Binding TerminationReason}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
