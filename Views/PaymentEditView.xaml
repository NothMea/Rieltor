<UserControl x:Class="WpfApp1.Views.PaymentEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="650">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Информация о платеже" FontSize="20" FontWeight="Bold" 
                   Foreground="#2C5F7F" Margin="0,0,0,20"/>

        <ScrollViewer Grid.Row="1">
            <StackPanel>
                <!-- Выбор договора -->
                <TextBlock Text="Договор аренды *" FontSize="14" FontWeight="SemiBold" Foreground="#2C5F7F"/>
                <ComboBox x:Name="CmbLease" DisplayMemberPath="DisplayInfo" 
                          SelectedValuePath="LeaseID" Margin="0,5,0,15"/>

                <!-- Дата платежа -->
                <TextBlock Text="Дата платежа *" FontSize="14" FontWeight="SemiBold" Foreground="#2C5F7F"/>
                <DatePicker x:Name="DtpPaymentDate" Margin="0,5,0,15" 
                            Style="{StaticResource {x:Type DatePicker}}"/>

                <!-- Сумма -->
                <TextBlock Text="Сумма платежа (₽) *" FontSize="14" FontWeight="SemiBold" Foreground="#2C5F7F"/>
                <TextBox x:Name="TxtAmount" Margin="0,5,0,15" PreviewTextInput="TxtAmount_PreviewTextInput"/>

                <!-- Статус -->
                <TextBlock Text="Статус *" FontSize="14" FontWeight="SemiBold" Foreground="#2C5F7F"/>
                <ComboBox x:Name="CmbStatus" Margin="0,5,0,15">
                    <ComboBoxItem Content="Оплачен"/>
                    <ComboBoxItem Content="В ожидании"/>
                    <ComboBoxItem Content="Просрочен"/>
                    <ComboBoxItem Content="Отменен"/>
                </ComboBox>

                <!-- Примечание -->
                <TextBlock Text="Примечание" FontSize="14" FontWeight="SemiBold" Foreground="#2C5F7F"/>
                <TextBox x:Name="TxtNotes" Margin="0,5,0,15" TextWrapping="Wrap" AcceptsReturn="True" 
                         VerticalScrollBarVisibility="Auto" Height="80"/>
            </StackPanel>
        </ScrollViewer>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Отмена" Click="BtnCancel_Click" Width="120" Background="#95A5A6"/>
            <Button Content="Сохранить" Click="BtnSave_Click" Width="120" Margin="10,0,0,0"/>
        </StackPanel>
    </Grid>
</UserControl>
