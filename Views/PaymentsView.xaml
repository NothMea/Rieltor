<UserControl x:Class="WpfApp1.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">
    <UserControl.Resources>
        <local:StatusColorConverter x:Key="StatusColorConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок и фильтры -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="SpaceBetween" Margin="0,0,0,15">
            <TextBlock Text="Управление платежами" FontSize="24" FontWeight="Bold" Foreground="#2C5F7F"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,0,0,0">
                <TextBlock Text="Фильтр по договору:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbLeaseFilter" Width="200" Margin="0,0,15,0" SelectionChanged="CmbLeaseFilter_SelectionChanged"/>
                <TextBlock Text="Статус:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="CmbStatusFilter" Width="150" SelectionChanged="CmbStatusFilter_SelectionChanged">
                    <ComboBoxItem Content="Все"/>
                    <ComboBoxItem Content="Оплачен"/>
                    <ComboBoxItem Content="В ожидании"/>
                    <ComboBoxItem Content="Просрочен"/>
                </ComboBox>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Добавить платеж" Click="BtnAddPayment_Click" Width="150" Height="35" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="Отметить как оплаченный" Click="BtnMarkAsPaid_Click" Width="170" Height="35" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}" Background="#4CAF50"/>
            <Button Content="Удалить" Click="BtnDeletePayment_Click" Width="120" Height="35" Style="{StaticResource TabButtonStyle}" Background="#f44336"/>
        </StackPanel>

        <!-- Таблица платежей -->
        <DataGrid x:Name="DgPayments" Grid.Row="2" AutoGenerateColumns="False" 
                  CanUserAddRows="False" CanUserDeleteRows="False"
                  IsReadOnly="True" SelectionMode="Single"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                  AlternatingRowBackground="#F5F5F5" RowHeight="35">
            <DataGrid.Columns>
                <DataGridTextColumn Header="№" Binding="{Binding PaymentID}" Width="50"/>
                <DataGridTextColumn Header="Номер договора" Binding="{Binding Leases.LeaseNumber}" Width="120"/>
                <DataGridTextColumn Header="Дата платежа" Binding="{Binding PaymentDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                <DataGridTextColumn Header="Сумма (₽)" Binding="{Binding Amount, StringFormat={}{0:N2}}" Width="100"/>
                <DataGridTemplateColumn Header="Статус" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="8,4" 
                                    Background="{Binding Status, Converter={StaticResource StatusColorConverter}}"
                                    HorizontalAlignment="Left">
                                <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Примечание" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Итоговая информация -->
        <Border Grid.Row="3" Background="#E3F2FD" CornerRadius="8" Padding="15" Margin="0,15,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Всего платежей:" FontWeight="Bold" Foreground="#2C5F7F"/>
                    <TextBlock x:Name="TxtTotalPayments" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Общая сумма (₽):" FontWeight="Bold" Foreground="#2C5F7F"/>
                    <TextBlock x:Name="TxtTotalAmount" FontSize="18" FontWeight="Bold"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Просрочено платежей:" FontWeight="Bold" Foreground="#2C5F7F"/>
                    <TextBlock x:Name="TxtOverdueCount" FontSize="18" FontWeight="Bold" Foreground="#f44336"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>
