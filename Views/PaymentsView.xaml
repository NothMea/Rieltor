<UserControl x:Class="WpfApp1.Views.PaymentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,15,15,10">
            <Button Content="➕ Добавить платеж" Click="BtnAdd_Click" Width="180" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="🔄 Обновить" Click="BtnRefresh_Click" Width="120" Margin="10,0,0,0" Style="{StaticResource TabButtonStyle}"/>
            
            <!-- Фильтры -->
            <TextBlock Text="Статус:" VerticalAlignment="Center" Margin="20,0,5,0" Foreground="#2C5F7F" FontWeight="SemiBold"/>
            <ComboBox x:Name="CmbStatusFilter" Width="150" SelectionChanged="CmbStatusFilter_SelectionChanged">
                <ComboBoxItem Content="Все статусы" IsSelected="True"/>
                <ComboBoxItem Content="Оплачен"/>
                <ComboBoxItem Content="В ожидании"/>
                <ComboBoxItem Content="Просрочен"/>
                <ComboBoxItem Content="Отменен"/>
            </ComboBox>
            
            <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="15,0,5,0" Foreground="#2C5F7F" FontWeight="SemiBold"/>
            <TextBox x:Name="TxtSearch" Width="200" TextChanged="TxtSearch_TextChanged"/>
        </StackPanel>

        <!-- Сводная информация -->
        <Border Grid.Row="1" Margin="15,5,15,10" Background="#F5F9FB" CornerRadius="8" Padding="15" BorderBrush="#D0E1ED" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Всего платежей" Foreground="#2C5F7F" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtTotalPayments" Text="0" FontSize="22" FontWeight="Bold" Foreground="#1A5276"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Оплачено" Foreground="#27AE60" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtPaidAmount" Text="0 ₽" FontSize="22" FontWeight="Bold" Foreground="#1E8449"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="В ожидании" Foreground="#F39C12" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtPendingAmount" Text="0 ₽" FontSize="22" FontWeight="Bold" Foreground="#D68910"/>
                </StackPanel>
                
                <StackPanel Grid.Column="3">
                    <TextBlock Text="Просрочено" Foreground="#E74C3C" FontSize="13" FontWeight="SemiBold"/>
                    <TextBlock x:Name="TxtOverdueAmount" Text="0 ₽" FontSize="22" FontWeight="Bold" Foreground="#C0392B"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Список платежей -->
        <ScrollViewer Grid.Row="2" Margin="15,0,15,15">
            <DataGrid x:Name="PaymentsGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                      HeadersVisibility="Column" GridLinesVisibility="Horizontal" BorderThickness="0"
                      RowHeight="55" AlternatingRowBackground="#F5F5F5" SelectionMode="Single">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#2C5F7F"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,8"/>
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding PaymentID}" Width="50"/>
                    <DataGridTextColumn Header="№ Договора" Binding="{Binding Leases.LeaseNumber}" Width="100"/>
                    <DataGridTextColumn Header="Арендатор" Binding="{Binding Leases.Tenants.Name}" Width="150"/>
                    <DataGridTextColumn Header="Объект" Binding="{Binding Leases.Property.Address}" Width="180"/>
                    <DataGridTextColumn Header="Дата платежа" Binding="{Binding PaymentDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                    <DataGridTextColumn Header="Сумма" Binding="{Binding Amount, StringFormat={}{0:N2} ₽}" Width="100"/>
                    
                    <DataGridTemplateColumn Header="Статус" Width="110">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="12" Padding="8,4" HorizontalAlignment="Center"
                                        Background="{Binding Status, Converter={StaticResource StatusColorConverter}}">
                                    <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" FontSize="12"
                                               HorizontalAlignment="Center"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTextColumn Header="Примечание" Binding="{Binding Notes}" Width="150"/>
                    
                    <DataGridTemplateColumn Header="Действия" Width="140">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                    <Button Content="✏️" Click="EditPaymentButton_Click" 
                                            Width="32" Height="32" Margin="0,0,5,0" Tag="{Binding}"
                                            ToolTip="Редактировать"/>
                                    <Button Content="🗑️" Click="DeletePaymentButton_Click" 
                                            Width="32" Height="32" Tag="{Binding}"
                                            ToolTip="Удалить"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>
    </Grid>
</UserControl>
