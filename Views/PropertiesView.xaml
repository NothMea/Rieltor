<UserControl x:Class="WpfApp1.Views.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15">
            <Button Content="Добавить объект" Click="BtnAdd_Click" Width="140" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="Добавить договор" Click="BtnAddLease_Click" Width="140" Style="{StaticResource TabButtonStyle}"/>
        </StackPanel>

        <!-- Панель сортировки и фильтров -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="15,0,15,15">
            <TextBlock Text="Сортировка:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="Bold"/>
            <ComboBox x:Name="CmbSortStatus" Width="150" SelectionChanged="CmbSortStatus_SelectionChanged" Margin="0,0,15,0">
                <ComboBoxItem Content="Все статусы"/>
                <ComboBoxItem Content="Свободен"/>
                <ComboBoxItem Content="Сдан"/>
            </ComboBox>
            <ComboBox x:Name="CmbSortOccupancy" Width="150" SelectionChanged="CmbSortOccupancy_SelectionChanged" Margin="0,0,15,0">
                <ComboBoxItem Content="Все объекты"/>
                <ComboBoxItem Content="Свободные"/>
                <ComboBoxItem Content="Занятые"/>
            </ComboBox>
        </StackPanel>

        <!-- Карточки объектов -->
        <ScrollViewer Grid.Row="2" Margin="15,0,15,15">
            <ItemsControl x:Name="ItemsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="8" Margin="0,0,15,15" Padding="15" Width="520">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="110"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <!-- Изображение объекта -->
                                <Image Grid.Column="0" Width="90" Height="90" Stretch="UniformToFill" Margin="0,0,10,0" VerticalAlignment="Top">
                                    <Image.Source>
                                        <Binding Path="ImagePath">
                                            <Binding.TargetNullValue>
                                                <BitmapImage UriSource="/Resources/logo.png"/>
                                            </Binding.TargetNullValue>
                                            <Binding.FallbackValue>
                                                <BitmapImage UriSource="/Resources/logo.png"/>
                                            </Binding.FallbackValue>
                                        </Binding>
                                    </Image.Source>
                                </Image>

                                <!-- Информация об объекте -->
                                <StackPanel Grid.Column="1" Grid.Row="0" VerticalAlignment="Top">
                                    <TextBlock Text="{Binding Address}" FontSize="16" FontWeight="Bold" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding PropertyType}" FontStyle="Italic" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding Area, StringFormat=Площадь: {0} м²}" Margin="0,0,0,5"/>
                                    <TextBlock Text="{Binding MonthlyRent, StringFormat=Стоимость: {0} ₽/мес}" FontWeight="SemiBold" Margin="0,0,0,10"/>

                                    <Border x:Name="ActiveLeasePanel" Visibility="{Binding HasActiveLease, Converter={StaticResource BoolToVisibilityConverter}}" Background="#E8F4F8" Padding="8" Margin="0,5,0,5" CornerRadius="5">
                                        <StackPanel>
                                            <TextBlock Text="Активный договор:" FontWeight="Bold" Margin="0,0,0,5"/>
                                            <TextBlock Text="{Binding ActiveLease.LeaseNumber, StringFormat=№ {0}}" Margin="0,0,0,3"/>
                                            <TextBlock>
                                                <Run Text="С "/>
                                                <Run Text="{Binding ActiveLease.StartDate, StringFormat=dd.MM.yyyy}"/>
                                                <Run Text=" по "/>
                                                <Run Text="{Binding ActiveLease.EndDate, StringFormat=dd.MM.yyyy}"/>
                                            </TextBlock>
                                            <TextBlock Text="{Binding ActiveLease.PaymentStatus, StringFormat=Статус: {0}}" FontWeight="SemiBold"/>
                                            <!-- Кнопка редактирования договора -->
                                            <Button Content="Редактировать договор" Click="EditLeaseButton_Click" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="10,5"/>
                                        </StackPanel>
                                    </Border>
                                    <TextBlock x:Name="NoLeaseText" Text="❌ Нет активных договоров" Foreground="#CD5C5C" FontWeight="Bold" Visibility="{Binding HasActiveLease, Converter={StaticResource InverseBoolToVisibilityConverter}}" Margin="0,5,0,0"/>
                                </StackPanel>

                                <!-- Кнопки управления объектом -->
                                <StackPanel Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                                    <Button Content="✏️ Редактировать" Click="EditPropertyButton_Click" Width="120" Margin="0,0,5,0"/>
                                    <Button Content="🗑️ Удалить" Click="DeletePropertyButton_Click" Width="100"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
