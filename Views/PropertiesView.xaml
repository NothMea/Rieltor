<UserControl x:Class="WpfApp1.Views.PropertiesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Панель управления -->
        <Border Grid.Row="0" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Margin="10">
            <StackPanel Orientation="Horizontal" Margin="15">
                <Button Content="➕ Добавить объект" Click="BtnAdd_Click" Width="160" Margin="0,0,15,0" Padding="10,8" Background="#5B9BD5" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                <Button Content="📄 Добавить договор" Click="BtnAddLease_Click" Width="170" Padding="10,8" Background="#2C5F7F" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <!-- Панель сортировки и фильтров -->
        <Border Grid.Row="1" Background="White" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="8" Margin="10,0,10,10">
            <StackPanel Orientation="Horizontal" Margin="15,10">
                <TextBlock Text="🔍 Фильтры:" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="Bold" Foreground="#2C5F7F" FontSize="14"/>
                <TextBlock Text="Статус:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <ComboBox x:Name="CmbSortStatus" Width="160" SelectionChanged="CmbSortStatus_SelectionChanged" Margin="0,0,20,0" Padding="8,5" Background="#F5F5F5" BorderBrush="#CCCCCC"/>
                <TextBlock Text="Занятость:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                <ComboBox x:Name="CmbSortOccupancy" Width="160" SelectionChanged="CmbSortOccupancy_SelectionChanged" Margin="0,0,0,0" Padding="8,5" Background="#F5F5F5" BorderBrush="#CCCCCC"/>
            </StackPanel>
        </Border>

        <!-- Карточки объектов -->
        <ScrollViewer Grid.Row="2" Margin="10">
            <ItemsControl x:Name="ItemsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" HorizontalAlignment="Left"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="10" Margin="10" Padding="0" Width="600" Height="320">
                            <Border Background="White" BorderBrush="#CCCCCC" BorderThickness="1" CornerRadius="10" Margin="10" Padding="15" Width="538" Height="319">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <!-- Изображение объекта -->
                                    <Border Grid.Column="0" Grid.Row="0" Background="#F5F5F5" Margin="10" CornerRadius="8">
                                        <Image Width="160" Height="160" Stretch="UniformToFill" VerticalAlignment="Center" HorizontalAlignment="Center">
                                            <Image.Source>
                                                <Binding Path="ImagePath">
                                                    <Binding.TargetNullValue>
                                                        <BitmapImage UriSource="/Resources/logo.png"/>
                                                    </Binding.TargetNullValue>
                                                    <Binding.FallbackValue>
                                                        <BitmapImage UriSource="/Resources/logo.png"/>
                                                    </Binding.FallbackValue>
                                                </Binding>
                                            </Image.Source>
                                        </Image>
                                    </Border>

                                    <!-- Информация об объекте -->
                                    <ScrollViewer Grid.Column="1" Grid.Row="0" VerticalScrollBarVisibility="Auto">
                                        <StackPanel Margin="15,15,15,5" VerticalAlignment="Top">
                                            <TextBlock Text="{Binding Address}" FontSize="18" FontWeight="Bold" Margin="0,0,0,8" TextWrapping="Wrap" Foreground="#2C5F7F"/>
                                            <TextBlock Text="{Binding PropertyType}" FontStyle="Italic" Margin="0,0,0,8" Foreground="#555555"/>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                                                <TextBlock Text="📐 " FontSize="14"/>
                                                <TextBlock Text="{Binding Area, StringFormat={}{0} м²}" Margin="0,0,0,8" Foreground="#333333"/>
                                            </StackPanel>
                                            <StackPanel Orientation="Horizontal" Margin="0,0,0,15">
                                                <TextBlock Text="💰 " FontSize="14"/>
                                                <TextBlock Text="{Binding MonthlyRent, StringFormat={}{0:N0} ₽/мес}" FontWeight="SemiBold" Foreground="#2C5F7F"/>
                                            </StackPanel>

                                            <Border x:Name="ActiveLeasePanel" Visibility="{Binding HasActiveLease, Converter={StaticResource BoolToVisibilityConverter}}" Background="#E8F4F8" Padding="12" Margin="0,5,0,10" CornerRadius="6" BorderBrush="#5B9BD5" BorderThickness="1">
                                                <StackPanel>
                                                    <TextBlock Text="📄 Активный договор:" FontWeight="Bold" Margin="0,0,0,8" Foreground="#2C5F7F"/>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                        <TextBlock Text="№ " FontWeight="Bold"/>
                                                        <TextBlock Text="{Binding ActiveLease.LeaseNumber}" Foreground="#333333"/>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                                        <TextBlock Text="📅 " FontSize="12"/>
                                                        <TextBlock Margin="0,0,0,0" Foreground="#555555">
                                                        <Run Text="С "/>
                                                        <Run Text="{Binding ActiveLease.StartDate, StringFormat=dd.MM.yyyy}"/>
                                                        <Run Text=" по "/>
                                                        <Run Text="{Binding ActiveLease.EndDate, StringFormat=dd.MM.yyyy}"/>
                                                        </TextBlock>
                                                    </StackPanel>
                                                    <StackPanel Orientation="Horizontal">
                                                        <TextBlock Text="📊 Статус: " FontWeight="Bold" FontSize="12"/>
                                                        <TextBlock Text="{Binding ActiveLease.PaymentStatus}" FontWeight="SemiBold" Foreground="#2C5F7F" FontSize="12"/>
                                                    </StackPanel>
                                                    <!-- Кнопка редактирования договора -->
                                                    <Button Content="✏️ Редактировать договор" Click="EditLeaseButton_Click" HorizontalAlignment="Left" Margin="0,12,0,0" Padding="12,6" Background="#5B9BD5" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                                </StackPanel>
                                            </Border>
                                            <Border x:Name="NoLeasePanel" Visibility="{Binding HasActiveLease, Converter={StaticResource InverseBoolToVisibilityConverter}}" Background="#FFF8E1" Padding="12" Margin="0,5,0,10" CornerRadius="6" BorderBrush="#FFD54F" BorderThickness="1">
                                                <StackPanel Orientation="Horizontal">
                                                    <TextBlock Text="⚠️ " FontSize="16" VerticalAlignment="Center"/>
                                                    <TextBlock Text="Нет активных договоров" Foreground="#CD5C5C" FontWeight="Bold" VerticalAlignment="Center"/>
                                                </StackPanel>
                                            </Border>
                                        </StackPanel>
                                    </ScrollViewer>

                                    <!-- Кнопки управления объектом -->
                                    <Border Grid.Column="0" Grid.ColumnSpan="2" Grid.Row="1" Background="#F9F9F9" Padding="15" BorderBrush="#EEEEEE" BorderThickness="0,1,0,0">
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                            <Button Content="✏️ Редактировать" Click="EditPropertyButton_Click" Width="140" Margin="0,0,10,0" Padding="8,6" Background="#5B9BD5" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                            <Button Content="🗑️ Удалить" Click="DeletePropertyButton_Click" Width="120" Padding="8,6" Background="#CD5C5C" Foreground="White" BorderThickness="0" Cursor="Hand"/>
                                        </StackPanel>
                                    </Border>
                                </Grid>
                            </Border>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
