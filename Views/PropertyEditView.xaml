<UserControl x:Class="WpfApp1.Views.PropertyEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfApp1.Views"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding TitleText}" FontSize="24" FontWeight="Bold" Margin="0,0,0,20" Grid.ColumnSpan="2"/>

        <!-- Адрес -->
        <TextBlock Text="Адрес:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtAddress" Grid.Row="1" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Тип объекта -->
        <TextBlock Text="Тип объекта:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbPropertyType" Grid.Row="2" Grid.Column="1" Height="30" Margin="0,5">
            <ComboBoxItem Content="Офис"/>
            <ComboBoxItem Content="Склад"/>
            <ComboBoxItem Content="Торговое помещение"/>
            <ComboBoxItem Content="Производственное помещение"/>
        </ComboBox>

        <!-- Площадь -->
        <TextBlock Text="Площадь (м²):" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtArea" Grid.Row="3" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Стоимость аренды -->
        <TextBlock Text="Стоимость аренды (₽/мес):" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="TxtMonthlyRent" Grid.Row="4" Grid.Column="1" Height="30" Margin="0,5"/>

        <!-- Статус -->
        <TextBlock Text="Статус:" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="CmbStatus" Grid.Row="5" Grid.Column="1" Height="30" Margin="0,5">
            <ComboBoxItem Content="Свободен"/>
            <ComboBoxItem Content="Занят"/>
            <ComboBoxItem Content="На обслуживании"/>
        </ComboBox>

        <!-- Изображение -->
        <TextBlock Text="Изображение:" Grid.Row="6" Grid.Column="0" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="6" Grid.Column="1" Orientation="Horizontal" Margin="0,5">
            <TextBox x:Name="TxtImagePath" Width="300" Height="30" IsReadOnly="True"/>
            <Button Content="Обзор" Click="BtnBrowse_Click" Margin="10,0,0,0" Width="80"/>
            <Image x:Name="ImgPreview" Width="100" Height="70" Margin="10,0,0,0" Stretch="UniformToFill"/>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Orientation="Horizontal" Grid.Row="8" Grid.ColumnSpan="2" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Сохранить" Click="BtnSave_Click" Width="120" Margin="0,0,10,0" Style="{StaticResource TabButtonStyle}"/>
            <Button Content="Отмена" Click="BtnCancel_Click" Width="120" Style="{StaticResource TabButtonStyle}"/>
        </StackPanel>
    </Grid>
</UserControl>
